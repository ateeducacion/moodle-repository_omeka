{
  "$schema": "https://raw.githubusercontent.com/ateeducacion/moodle-playground/refs/heads/main/assets/blueprints/blueprint-schema.json",
  "preferredVersions": { "php": "8.3", "moodle": "5.0" },
  "landingPage": "/user/files.php",
  "constants": {
    "ADMIN_USER": "admin",
    "ADMIN_PASS": "test",
    "ADMIN_EMAIL": "admin@example.com",
    "OMEKA_BASEURL": "https://dev.omeka.org/omeka-s-sandbox"
  },
  "steps": [
    {
      "step": "installMoodle",
      "options": {
        "adminUser": "{{ADMIN_USER}}",
        "adminPass": "{{ADMIN_PASS}}",
        "adminEmail": "{{ADMIN_EMAIL}}",
        "siteName": "Omeka Repository Demo",
        "locale": "es",
        "timezone": "Europe/Madrid"
      }
    },
    { "step": "login", "username": "{{ADMIN_USER}}" },
    {
      "step": "installMoodlePlugin",
      "pluginType": "repository",
      "pluginName": "omeka",
      "url": "https://github.com/ateeducacion/moodle-repository_omeka/archive/refs/heads/main.zip"
    },
    {
      "step": "runPhpCode",
      "code": "if (!defined('CLI_SCRIPT')) { define('CLI_SCRIPT', true); } require('/www/moodle/config.php'); try { $now = time(); $existing = $DB->get_record('repository', ['type' => 'omeka']); if ($existing) { $typeid = (int)$existing->id; $DB->set_field('repository', 'visible', 1, ['id' => $typeid]); } else { $max = (int)$DB->get_field_sql('SELECT MAX(sortorder) FROM {repository}'); $rec = (object)['type' => 'omeka', 'visible' => 1, 'sortorder' => $max + 1]; $typeid = (int)$DB->insert_record('repository', $rec); } set_config('enablecourseinstances', 1, 'omeka'); set_config('enableuserinstances', 1, 'omeka'); $systemcontext = context_system::instance(); $exinst = $DB->get_record('repository_instances', ['typeid' => $typeid, 'contextid' => $systemcontext->id]); if (!$exinst) { $inst = (object)['name' => 'Omeka Sandbox', 'typeid' => $typeid, 'userid' => 0, 'contextid' => $systemcontext->id, 'username' => '', 'password' => '', 'timecreated' => $now, 'timemodified' => $now, 'readonly' => 0]; $instanceid = (int)$DB->insert_record('repository_instances', $inst); foreach (['baseurl' => '{{OMEKA_BASEURL}}', 'siteid' => '0', 'keyidentity' => '', 'keycredential' => '', 'acceptedtypes' => '', 'sitelabel' => '', 'siteslug' => ''] as $n => $v) { $DB->insert_record('repository_instance_config', (object)['instanceid' => $instanceid, 'name' => $n, 'value' => $v]); } echo \"omeka enabled+visible: typeid={$typeid}, instanceid={$instanceid}\\n\"; } else { echo \"omeka exists: typeid={$typeid}, instanceid={$exinst->id}\\n\"; } } catch (\\Throwable $e) { echo 'ERROR: ' . $e->getMessage() . \"\\n\"; }"
    },
    {
      "step": "createCategory",
      "name": "Test Courses"
    },
    {
      "step": "createCourse",
      "fullname": "Omeka Repository Test Course",
      "shortname": "OMEKA01",
      "category": "Test Courses",
      "summary": "Test course for the Omeka-S repository plugin. The Omeka Sandbox repository instance is preconfigured against {{OMEKA_BASEURL}}."
    },
    {
      "step": "createUser",
      "username": "student",
      "password": "test",
      "email": "student@example.com",
      "firstname": "Demo",
      "lastname": "Student"
    },
    {
      "step": "enrolUser",
      "username": "{{ADMIN_USER}}",
      "course": "OMEKA01",
      "role": "editingteacher"
    },
    {
      "step": "enrolUser",
      "username": "student",
      "course": "OMEKA01",
      "role": "student"
    },
    { "step": "setLandingPage", "path": "/user/files.php" }
  ]
}
